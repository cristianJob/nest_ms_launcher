apiVersion: v1
kind: Service
metadata:
  labels:
    app: orders-db
  name: orders-db
spec:
  ports:
  - name: "5432"
    port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: orders-db
  type: ClusterIP
status:
  loadBalancer: {}
